import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  actionsView: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  actionsLeftView: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 20,
  },
})
