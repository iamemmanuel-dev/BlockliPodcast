import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  inputView: {
    height: 56,
    borderRadius: 16,
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 10,
    paddingHorizontal: 15,
    marginBottom: 20,
  },
})
